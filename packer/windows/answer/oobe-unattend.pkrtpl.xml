<?xml version="1.0" encoding="utf-8"?>
<!--
  Post-sysprep answer file. A provisioner copies this to
  C:\Windows\Panther\unattend.xml before running sysprep /generalize, and
  Windows consumes it automatically on the first boot of every clone.

  Without it a generalized template is unmanageable: /generalize strips the
  local Administrator password, the WinRM listener and the RDP settings the
  build configured, so clones would boot to an interactive OOBE prompt that no
  automation can answer.

  Deliberately no <ComputerName>: leaving it unset makes Windows generate a
  unique name per clone, which is the point of generalizing. Ansible sets the
  real hostname afterwards from the published inventory.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend" xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-International-Core" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <NetworkLocation>Work</NetworkLocation>
        <ProtectYourPC>3</ProtectYourPC>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
      </OOBE>
      <UserAccounts>
        <AdministratorPassword>
          <Value>${admin_password}</Value>
          <PlainText>true</PlainText>
        </AdministratorPassword>
      </UserAccounts>
      <!--
        Load-bearing, not a convenience. FirstLogonCommands run at the first
        interactive logon and nowhere else, so without an autologon nobody ever
        logs in, none of the commands below execute, and the clone comes up with
        neither WinRM nor RDP - unreachable, which is precisely the failure this
        file exists to prevent. LogonCount 1 spends the autologon on that single
        boot and Windows clears it afterwards.
      -->
      <AutoLogon>
        <Password>
          <Value>${admin_password}</Value>
          <PlainText>true</PlainText>
        </Password>
        <Enabled>true</Enabled>
        <LogonCount>1</LogonCount>
        <Username>Administrator</Username>
      </AutoLogon>
      <FirstLogonCommands>
        <!--
          Re-establish remote management on the clone. The guest agent and
          virtio drivers survive generalize (they are installed software, not
          configuration), so only the listeners and firewall rules are rebuilt
          here. Ansible's windows_baseline role enforces the same end state
          idempotently afterwards - this exists so the guest is reachable
          enough for Ansible to connect at all.
        -->
        <!--
          First, and load-bearing for both WinRM and RDP. Windows classifies an
          unidentified network as Public; the built-in WinRM and Remote Desktop
          rules for that profile are scoped to LocalSubnet, so a clone would
          listen on both ports and silently drop every connection from another
          subnet - Ansible and any RDP client that is not on the guest's own
          VLAN. winrm quickconfig also refuses outright on a Public network.
          Retried because the NIC may not be up at the instant of first logon.
        -->
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <CommandLine>powershell.exe -NoProfile -Command "1..30 | ForEach-Object { $prof = Get-NetConnectionProfile -ErrorAction SilentlyContinue; if ($prof) { $prof | Set-NetConnectionProfile -NetworkCategory Private; break }; Start-Sleep -Seconds 2 }"</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>2</Order>
          <CommandLine>cmd /c winrm quickconfig -quiet</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>3</Order>
          <CommandLine>cmd /c winrm set winrm/config/service/auth @{Basic="true"}</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>4</Order>
          <CommandLine>cmd /c winrm set winrm/config/service @{AllowUnencrypted="true"}</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>5</Order>
          <CommandLine>cmd /c netsh advfirewall firewall set rule group="Windows Remote Management" new enable=yes</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>6</Order>
          <CommandLine>cmd /c reg add "HKLM\System\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>7</Order>
          <CommandLine>cmd /c netsh advfirewall firewall set rule group="remote desktop" new enable=yes</CommandLine>
        </SynchronousCommand>
      </FirstLogonCommands>
    </component>
  </settings>

</unattend>
